.botonera
  -# FIXME en vez de sacar el .object del decorador, dejar el modelo sin decorar accesible
  - if can?(:update, @perfil.object) || can?(:modificar, @perfil.object)
    = link_to 'Editar', edit_perfil_path(@perfil)
    = '|'
    = link_to 'Permisos', permisos_path(modelo: 'perfil', id: @perfil)
    = '|'
  -# FIXME en vez de sacar el .object del decorador, dejar el modelo sin decorar accesible
  - if can? :read, @perfil.object
    = link_to 'Mostrar analíticos', perfil_analiticos_path(@perfil)
    = '|'
  -# FIXME en vez de sacar el .object del decorador, dejar el modelo sin decorar accesible
  -# FIXME => ??
  - if can? :read, @perfil.object => Adjunto
    = link_to 'Mostrar adjuntos', perfil_adjuntos_path(@perfil)
    = '|'
  = link_to 'Volver', perfiles_path

-# TODO issue #41
= render partial: ficha, locals: { perfil: @perfil, bloqueado: true }

= volver_arriba
