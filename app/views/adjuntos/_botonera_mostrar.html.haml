- if can? :read, @perfil.object
  = link_to 'Mostrar perfil', @perfil
  = '|'
- if can? :update, @adjunto.object
  = link_to 'Editar adjunto', edit_perfil_adjunto_path(@perfil, @adjunto)
  = '|'
= link_to 'Volver', perfil_adjuntos_path
