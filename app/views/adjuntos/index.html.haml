.botonera= render 'botonera_indice'

#lista.span-24
  %table#adjuntos.centrado.span-23.last
    %thead
      %tr
        %th Archivo
        %th Tipo
        %th Tamaño
        %th Notas
        %th.acciones

    %tbody
      = render @adjuntos

.botonera= render 'botonera_indice'
