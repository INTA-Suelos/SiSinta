%li.page-item{ class: current_page.last? ? 'disabled' : '' }
  = link_to t('views.pagination.last.icon'), url,
    remote: remote,
    class: 'page-link',
    title: t('views.pagination.last.text'),
    disabled: current_page.last?
