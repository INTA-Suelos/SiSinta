%li.page-item{ class: current_page.first? ? 'disabled' : '' }
  = link_to t('views.pagination.previous.icon'), url,
    rel: 'prev',
    remote: remote,
    class: 'page-link',
    title: t('views.pagination.previous.text'),
    disabled: current_page.first?
